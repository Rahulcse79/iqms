/**
 * IQMS Application Breakpoints Configuration
 * Responsive breakpoints matching original CSS media queries
 */

const breakpoints = {
  values: {
    xs: 0,       // Mobile phones
    sm: 480,     // Small tablets (original: @media max-width: 480px)
    md: 640,     // Tablets (original: @media max-width: 640px)
    lg: 920,     // Laptops (original: @media max-width: 920px) - sidebar overlay
    xl: 1024,    // Desktops (original: @media max-width: 1024px)
    xxl: 1200,   // Large desktops
  },
};

export default breakpoints;
